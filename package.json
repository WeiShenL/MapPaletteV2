{
  "name": "mappalettev2",
  "version": "2.0.0",
  "description": "MapPaletteV2 - Social route mapping platform",
  "private": true,
  "scripts": {
    "setup": "docker compose down -v && npm install && docker compose up -d --build supabase-db redis && docker compose up -d --build user-service && docker compose exec -T user-service sh -c \"cd /app/shared && npx prisma migrate deploy --schema prisma/schema.prisma\" && docker compose up -d",
    "services:start": "docker compose up -d",
    "services:stop": "docker compose down",
    "services:logs": "docker compose logs -f",
    "services:ps": "docker compose ps"
  },
  "keywords": [
    "route",
    "mapping",
    "social",
    "docker",
    "prisma",
    "postgresql"
  ],
  "author": "MapPaletteV2 Contributors",
  "license": "MIT",
  "devDependencies": {
    "@prisma/client": "^7.0.0",
    "dotenv": "^17.0.0",
    "prisma": "^7.0.0"
  }
}