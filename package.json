
{
  "name": "mappalettev2",
  "version": "2.0.0",
  "description": "MapPaletteV2 - Social route mapping platform",
  "private": true,
  "scripts": {
    "setup": "docker compose down -v && npm install && docker compose up -d --build supabase-db redis && docker compose up -d --build prisma-init && docker compose up -d",
    "start": "docker compose up -d",
    "stop": "docker compose down",
    "logs": "docker compose logs -f",
    "ps": "docker compose ps"
  },
  "keywords": [
    "route",
    "mapping",
    "social",
    "docker",
    "prisma",
    "postgresql"
  ],
  "author": "MapPaletteV2 Contributors",
  "license": "MIT",
  "devDependencies": {
    "@prisma/client": "^7.0.0",
    "dotenv": "^17.0.0",
    "prisma": "^7.0.0"
  }
}